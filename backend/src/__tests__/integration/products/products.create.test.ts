// import request from 'supertest';
// import { isUuid } from 'uuidv4';
// import { parseISO, isToday } from 'date-fns';
// import app from '../../../app';

describe('Products Routes', () => {
  interface Product {
    name: string;
    description: string;
    ownerId: string;
    price: number;
    priceOld: number;
  }

  // Add a user and get its id.
  // Add two products for this user
  // Try add another product with the same name
  // Try add a product with price_old higher than price
  // List all products from the user
});
